---
name: douni-weekly-publication-reconciliation
description: 每周核对上一自然周各机构执行表与抖音、小红书及独立端客户发布表，定位漏登、错归机构、昵称/平台/跨月日期/作品差异。同月日期差异不提醒。用于“周发布核对”“机构发布和客户表对账”“查上周发布差异”。
---

# 每周发布账实核对

## 公共业务配置

本 Skill 保留处理流程、例外和确认门禁，不单独维护客户表地址、周文档、机构色值或调度时间。先定位豆尼仓库（包含 `config/business-sources.json`），在仓库目录只读执行：

```powershell
node scripts/business-context.mjs --skill douni-weekly-publication-reconciliation
```

需要周执行文档时加 `--date YYYY-MM-DD`。阅读返回的来源、公共口径和本 Skill 写入边界；若运行时已注入 `runtime/business-context.json`，直接读取该快照，不另取一份不同版本。源缺失时停止相关操作，不能翻旧文档猜地址。手动和定时均调用原执行器，配置不授予新的写表或外发权限。

修改来源在仓库 `config/business-sources.json`，修改共用专项/色块/权威口径在 `config/business-policy.json`。调度以现有 Daemon 为准。脱离仓库独立打包时必须带上述配置和执行依赖、注明快照日期；不能把本 Skill 单独复制后视为可运行。Skill 自带命令仍在其原目录执行；包装器通过 `DOUNI_WORKSPACE` 或当前仓库目录定位执行器，不依赖固定盘符。

## 目标

把客户发布表中的真实发布与 13 张机构执行表中的“已发布”记录做双向核对。默认范围为上海时间上一自然周（周一至周日）。这是只读审计，不修改任何业务表。

## 强制流程

1. 完整读取 [references/reconciliation-rules.md](references/reconciliation-rules.md)。
2. 客户来源固定为原抖音、小红书客户发布表及新【独立端-内容发布】；机构来源固定为部门 Base 中动态发现的全部“机构 X 焦尾 执行表”，当前基线 13 张。独立端表只读，不补行。
3. 客户表有、机构表没有或未标“已发布”，以及机构表有、客户表没有，都属于差异；不得只做单向检查。先用完整客户记录作为匹配证据，再限定本周报告对象，避免周日发布、下周补登记被误报为漏登；周外无关记录不计入本周。
4. 匹配优先级为作品 ID/GID、规范化真实发布链接，再到同机构+同平台+规范化达人名+发布日期。短链、富文本显示形式、大小写或空格不同不能直接判差异。
5. 同名、多期或证据冲突时标记“待人工确认”，禁止猜测。
6. 只比较发布事实与归属，日期规则读取共享配置 `weeklyPublicationReconciliation`：同一公历年月内无论差几天均不提醒，跨月（含跨年）单列，不改任何原始日期。同作品的昵称差异归为登记差异，不当作漏登，也不靠删“的”等字符合并达人。互动数据不参与本审计。
7. 有差异或待核才向“焦尾-豆包用增”发送 Card 2.0；完全一致时不打扰群里。卡片必须逐条展示机构、达人、平台、差异点，并提供可点击的机构表和客户表入口。
8. 来源读取不完整、机构表少于 13 张或任一平台读取失败时，本次任务失败；不得把“没读到”写成“无差异”。

## 调度

- 执行时刻和启停读取 Daemon 当前计划，本 Skill 不维护时间副本。
- 每次核对上一自然周。
- 与机构次日进度检查、爆款复盘独立；其中一个失败不得篡改另一个的结果。
- 同一周、同一结果使用稳定幂等键，避免重复发相同卡片。

## 输出

- 核对周期；
- 客户表周内发布数、机构表周内已发布数、核对通过数、差异数、待核数；周外客户补登记只作为匹配证据，另注明关联数；
- 逐条差异：机构、达人、平台、客户表事实、机构表事实和具体不同点；
- 无差异时只在任务记录中写“核对一致”，不发群。
